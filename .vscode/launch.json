{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CMake: Debug Target", // Generic name
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${command:cmake.launchTargetDirectory}",
            "preLaunchTask": "CMake: Build"
        }
    ]
}